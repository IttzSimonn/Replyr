// Replace this URL with your deployed backend URL once you deploy to Render (or any host).
// For local testing: use your machine's local IP, e.g. "http://192.168.1.100:8000"
export const BACKEND_URL = 'https://replyr.onrender.com';
